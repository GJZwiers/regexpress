jobs:
  build:
    docker:
      - image: circleci/node:12.13.0

    steps:
      - checkout
      - run: npm install
      - run: npx eslint src/
      - run: npm test
workflows:
  version: 2
  build_and_test:
    jobs:
      - build